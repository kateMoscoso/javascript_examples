var age = 28

// edad = edad + 1
age += 1

var weigth = 65

// peso = peso - 2
weigth -= 2

var food = 1

peso += food

var running = 3

peso -= running

var wineValue = 200.3

var total = Math.round(wineValue * 100 * 3) / 100
var totalStr = total.toFixed(3)
var total2 = parseFloat(totalStr)

var pizza = 8
var people = 2
var pizzaSlicePerPerson = pizza / people